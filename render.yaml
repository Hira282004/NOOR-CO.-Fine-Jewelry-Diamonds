services:
  - type: web
    name: noor-jewelry-store
    env: node
    buildCommand: npm run build && cd backend && npm install
    startCommand: node backend/server.js
    envVars:
      - key: NODE_ENV
        value: production
